#if !defined( BlOOM_FXH )
#define BlOOM_FXH

float g_BloomThreshold = 0.20;			// Get the threshold of what brightness level we want to glow
float g_BloomOriginalIntensity = 1.0;	// Controls the Intensity of the original scene texture
float g_BloomSaturation = 1.5;			// Saturation amount on bloom
float g_BloomOriginalSaturation = 1.0;	// Saturation amount on original texture
float g_BloomIntensity = 2.0;			// Controls the Intensity of the bloom texture

#endif // !defined( BlOOM_FXH )